spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pretapreter?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: EE
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
    port: 8090
